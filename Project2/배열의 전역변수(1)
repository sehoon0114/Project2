#include <stdio.h>

#define MAX 10

int arr[MAX];

int main() {
    int i;

    // 배열 초기화
    for (i = 0; i < MAX; i++) {
        arr[i] = i + 1;
    }

    // 배열 출력
    for (i = 0; i < MAX; i++) {
        printf("%d\n", arr[i]);
    }

    return 0;
}
